on:
  push:
    branches:    
      - master
name: Build and lint
jobs:
  test:
    name: Build and lint
    runs-on: macOS-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: Build
        run: |
          swift build
          swiftlint